# EHF document ingestion ClamAV daemon configuration.
# The daemon socket is private to the EHF service account; document names and
# scan output are not forwarded to application logs.
LocalSocket /run/clamav/clamd.ctl
LocalSocketMode 0666
FixStaleSocket yes
Foreground yes
LogTime yes
DatabaseDirectory /var/lib/clamav
